{
  "name": "personal-task-manager",
  "version": "1.0.0",
  "description": "Personal Task Manager Full Stack App",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "build-frontend": "cd frontend && npm install && npm run build",
    "copy-frontend": "cp -r frontend/dist/* backend/public/ 2>/dev/null || (mkdir -p backend/public && cp -r frontend/dist/* backend/public/)",
    "heroku-postbuild": "npm run build-frontend && npm run copy-frontend"
  },
  "cacheDirectories": [
    "frontend/node_modules",
    "backend/node_modules"
  ]
}
